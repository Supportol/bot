# Newsbot — зависимости проекта (Python 3.10+)
# Установка: python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt

# --- Telegram-бот (bot.py, handlers/*) ---
aiogram==3.29.1

# --- HTTP-клиент (парсеры, TEXT.ru API, скачивание обложек) ---
aiohttp==3.14.1

# --- База данных SQLite (database/db.py) ---
aiosqlite==0.22.1

# --- Конфигурация из .env (config.py) ---
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.2

# --- Парсинг новостей: RSS, HTML-карточки, API-ответы (services/news_parser.py) ---
feedparser==6.0.12
beautifulsoup4==4.15.0
lxml==6.1.1

# --- Извлечение полного текста статей по URL (services/text_extractor.py) ---
trafilatura==2.1.0

# --- Обработка обложек: resize, размытый фон, watermark (services/image_processor.py) ---
Pillow==12.2.0
